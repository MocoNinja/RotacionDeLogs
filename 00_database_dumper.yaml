- name:  Dump database {{ database }} to tmp dir {{ sql_dump_dir }}
  mysql_db:
    state: dump
    name: {{ database }}
    # Guarda el dumpeo en la carpeta/nombre_host.sql
    target: "{{ db_dump_dir }}/{{ inventory_hostname }}.sql"